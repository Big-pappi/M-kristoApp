"""
Settings package for the M-Kristo backend.

DJANGO_SETTINGS_MODULE defaults to config.settings.dev (see manage.py /
wsgi.py / asgi.py). Set DJANGO_ENV=production (or point
DJANGO_SETTINGS_MODULE at config.settings.prod directly) to use the
production settings instead.
"""
